{
  "framework": "nextjs",
  "installCommand": "cp -a website/. . && rm -f requirements.txt requirements-*.txt Pipfile Pipfile.lock pyproject.toml && npm install",
  "buildCommand": "npm run build"
}
